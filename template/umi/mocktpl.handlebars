import Mock from 'mockjs'
import Field from '../src/pages/{{dirName}}/Table/Field.js'

const data = Mock.mock({
    'list|10-50': [{
            {{#listItems}}
             {{&prop}}:'{{&create}}',
            {{/listItems}}
    }
        ],
});
const { list } = data;
const outData = list.slice(0, 20);
const outList = {
    code: 0,
    data: outData,
    length: list.length,
};
export default {
    'GET /{{flowKey}}/list': {
        code: 0,
        ...outList,
    },
    'POST /{{flowKey}}/add': {
        code: 0,
        data: {},
    },
    'POST /{{flowKey}}/edit': {
        code: 0,
        data: {},
    },
};
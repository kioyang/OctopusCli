/**
* api地址生成。
* @author {{author}}-@{{company}}
* @date {{date}}
*/
const api = (options) => {
  // const { id } = options;
  return {
    list: `/{{flowKey}}/list`,
    add: `/{{flowKey}}/add`,
    edit: `/{{flowKey}}/edit`,
    delete: `/{{flowKey}}/delete`
  };
};
export default api;

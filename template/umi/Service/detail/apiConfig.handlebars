/**
* api地址生成。
* @author {{author}}-@{{company}}
* @date {{date}}
*/
const api = (options) => {
  // const { id } = options;
  return {
    list: `{{&apiUrl}}`,
    add: `/{{flowKey}}Detail/add`,
    edit: `/{{flowKey}}Detail/edit`,
    delete: `{{&deleteUrl}}`,
     detailUrl: `{{&detailUrl}}`
  };
};
export default api;


export const formMap = {
    {{#formColumns}}
        {{key}}: '{{key}}', // {{keyName}}
    {{/formColumns}}
  }
  
  export const detailMap = {
       {{#tableColumns}}
        {{key}}: '{{key}}', // {{keyName}}
    {{/tableColumns}}
  }